---
export const prerender = false;

import DashboardLayout from "@/layouts/dashboard.layout.astro";

const { session } = Astro.locals;

if (!session) {
  return Astro.redirect("/auth/login");
}
---

<DashboardLayout title="Dashboard"> hi </DashboardLayout>
