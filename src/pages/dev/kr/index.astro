---
import { getBookableSlots } from "./_query";
---

<pre
  style={{
    whiteSpace: "pre-wrap",
    fontFamily: "monospace",
  }}>
  <!-- next month -->
  {JSON.stringify(await getBookableSlots(new Date(new Date().getFullYear(), new Date().getMonth() + 1, 1)), null, 2)}
  </pre>
